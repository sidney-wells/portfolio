import React from "react";
import { Header } from "../Header";
import "./onepage.css";
import { Button, Layout } from "../utils/index.js";

type User = {
  name: string;
};

export const OnePage: React.FC = (props) => {
  // let { content, children } = props;
  return <Button label="Button" />;
};
